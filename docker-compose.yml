services:
  mongodb:
    image: mongo:7
    container_name: happy-birthday-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: happy-birthday-agent
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
